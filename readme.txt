#Comando para iniciar o servidor (que deve ficar iniciado durante desenvolvimento)
#Obs, só rode em background se souber recuperar, porque se precisar de dar um restart no server fode

php artisan serve
